<UserControl x:Class="ToDoList.ManageTaskCate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToDoList"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid Background="White">
            <!-- ScrollViewer to enable scrolling for DataGrid -->
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <Grid>
                    <!-- DataGrid with white background and blue header -->
                    <DataGrid x:Name="TaskCategoriesDataGrid" AutoGenerateColumns="False" 
                          HorizontalAlignment="Stretch" VerticalAlignment="Top" 
                          Margin="10" CanUserAddRows="False" CanUserDeleteRows="True" 
                          SelectionMode="Single" IsReadOnly="False" Height="300"
                          AlternatingRowBackground="LightGray" 
                          Background="White" 
                          RowBackground="White">

                        <!-- Header style with blue background and white text -->
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="Blue"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Style>
                        </DataGrid.Resources>

                        <!-- Define columns -->
                        <DataGrid.Columns>
                            <!-- ID column (hidden or read-only) -->
                            <DataGridTextColumn Header="ID" Binding="{Binding ID}" IsReadOnly="True" Width="50"/>

                            <!-- Name column -->
                            <DataGridTextColumn Header="Category Name" Binding="{Binding Name}" Width="*"/>

                            <!-- Description column -->
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </ScrollViewer>

            <!-- StackPanel with buttons to manage task categories -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="10">
                <Button Content="Add Category" Width="100" Margin="5" Click="AddCategory_Click" 
                    Background="Blue" Foreground="White"/>
                <Button Content="Remove Category" Width="100" Margin="5" Click="RemoveCategory_Click" 
                    Background="Blue" Foreground="White"/>
                <Button Content="Save Changes" Width="100" Margin="5" Click="SaveChanges_Click" 
                    Background="Blue" Foreground="White"/>
                <Button Content="Update Selected" Width="120" Margin="5" Click="UpdateSelected_Click" 
                    Background="Blue" Foreground="White"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
